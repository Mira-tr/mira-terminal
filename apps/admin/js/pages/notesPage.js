import{initNoteForm}from"../features/notes/noteForm.js";import{exportPublicNotes}from"../features/notes/notePublicExport.js";import{exportBackupNotes,importBackupNotes}from"../features/notes/noteBackup.js";
initNoteForm();document.getElementById("publicExportBtn").addEventListener("click",exportPublicNotes);document.getElementById("backupExportBtn").addEventListener("click",exportBackupNotes);document.getElementById("backupImportBtn").addEventListener("click",()=>document.getElementById("backupImportInput").click());document.getElementById("backupImportInput").addEventListener("change",async event=>{const file=event.target.files[0];if(!file)return;try{if(await importBackupNotes(file)){alert("Backup読み込みが成功しました");location.reload();}}catch(error){alert(`Backup読み込みに失敗しました: ${error.message}`);}event.target.value="";});
